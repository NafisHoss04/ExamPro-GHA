name: Custom action

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: 'Greet user'
        uses: NafisHoss04/custom-github-actions@0.0.1
        with:
          name: 'Nafis'
